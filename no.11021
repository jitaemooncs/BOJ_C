#include <stdio.h>

int main(void)
{
    int case_number, a, b;
    
    // Prompt user
    scanf("%d", &case_number);
    
    for (int i = 1; i <= case_number; i++)
    {
        scanf("%d %d", &a, &b);
        printf("Case #%d: %d\n", i, a + b);
    }
    
    return 0;
}
