#include <stdio.h>

int main(void)
{
    int number;
    
    // Prompt user
    scanf("%d", &number);
    
    for (int i = number; i > 0; i--)
    {
        printf("%d\n", i);
    }
    
    return 0;
}
