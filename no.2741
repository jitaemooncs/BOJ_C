#include <stdio.h>

int main(void)
{
    int number;
    
    // Prompt user
    scanf("%d", &number);
    
    for (int i = 1; i <= number; i++)
    {
        printf("%d\n", i);
    }
    
    return 0;
}
