#include <stdio.h>

int main(void)
{
    int number, sum = 0;
    
    // Prompt user
    scanf("%d", &number);
    
    for (int i = 1; i <= number; i++)
    {
        sum += i;
    }
    
    printf("%d\n", sum);
    
    return 0;
}
