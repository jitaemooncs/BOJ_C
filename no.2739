#include <stdio.h>

int main(void)
{
    int number;
    
    // Prompt the user
    scanf("%d", &number);
    
    for (int i = 0; i < 9; i++)
    {
        printf("%d * %d = %d\n", number, i + 1, number * (i + 1));
    }
    
    return 0;
}
